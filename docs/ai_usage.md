# AI Usage
* In streamlit_app.py:
  * Had many issues with producing the pdf due to characters that fdpf2 had problems with, needed sanitizing functions for output text
  * Generating the logo we used in the presentation and on the site
  * Helping add the doctor GIF and the Loading... animation while the code ir running
* In react_loop.py:
  * Suggested the general structure of the code to achieve what we want